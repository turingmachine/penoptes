'/etc/*.conf':

'/etc': {
  exclude: [ 
    dumpdates,
    amdates
  ]
}

'/var/www': {
  include: '\.ht*',
  recursive: false
}

'services': {
  command: 'chkconfig --list | grep :on',
}

'/etc/*.conf': {
  comment: 'this is a comment applying to all .conf files in /etc'
}
